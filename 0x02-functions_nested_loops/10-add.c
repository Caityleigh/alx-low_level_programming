#include "main.h"
#include <stdio.h>

/**
 * add - Function that adds two integers and returns the result
 * @x - The the first number computed
 * @y - The the second number computed
 * Return: Always 0
 */

int add(int x, int y)

{
	return (x + y);

}
