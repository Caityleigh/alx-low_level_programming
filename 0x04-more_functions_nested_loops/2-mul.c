#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: First integer to compute.
 * @b: Second integer to compute.
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}


