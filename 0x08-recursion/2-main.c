#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 * Return: Always 0.
 */

int main(void)
{
	int a;

	a = _strlen_recursion("Dreams feel real while we're in them");
	printf("%d\n", a);
	return (0);
}
